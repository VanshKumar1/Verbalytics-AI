{
  "name": "verbalytics-ai",
  "version": "1.0.0",
  "description": "Verbalytics AI — Debate & Interview Practice Platform",
  "scripts": {
    "predev": "npx kill-port 5000 5173 || true",
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "kill": "npx kill-port 5000 5173"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
